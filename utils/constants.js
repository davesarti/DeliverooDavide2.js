// Costants used in A*
export const BASE_STEP_COST = 1;
export const MIN_EDGE_COST = 0.1;
export const PARCEL_REWARD_DISCOUNT = 0.2;

// Costants used in actions
export const GO_TO_TIMEOUT_MS = 5000;

